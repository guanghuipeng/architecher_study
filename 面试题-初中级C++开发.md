# 初中级 C/C++ 开发工程师 面试题库

> **使用说明**：每个问题标注了 **考察点**、**初级合格线**、**中级合格线**，便于面试中据答定档。核心原则——能讲清"为什么用 A 不用 B、A 的代价是什么、什么时候不适用"的是中级；只会背答案的是初级。
>
> **档位速判**
> | 档位 | 标志 |
> |---|---|
> | 初级合格 | 语言基础/资源/OOP/STL 答对，能写正确的拷贝构造、能用智能指针但不深讲、虚函数/vtable 说得清 |
> | 中级合格 | + 现代 C++（移动/lambda/模板）、并发（锁/原子/内存序），且能讲代价与权衡 |
> | 中级偏强 | + 有真实重构/调优案例，能结合自己项目讲取舍、工具链熟 |

---

## 一、一道全栈系统题（一道顶十道）

### 题：实现固定大小环形缓冲区（ring buffer），线程安全，并扩展

> **题面**：实现一个固定大小的环形缓冲区，一个生产者写、一个消费者读，要线程安全。然后回答：能不能不加锁？多个生产者多个消费者时又怎么办？

**追问链**：
1. 先写 SPSC（单生产者单消费者）带锁版：数组 + `head`/`tail` + mutex。
2. "每次写读都加锁，瓶颈在哪？能不能无锁？" → 引到 `atomic` + 内存序。
3. "为什么 SPSC 能无锁、MPMC 就不能照搬？" → 分水岭。
4. "head 和 tail 放一个结构体里，会有性能问题吗？" → 伪共享 / `alignas(64)`。
5. "buffer 满了选阻塞还是丢弃？什么场景选哪个？" → 权衡题。
6. （扩展）"包成 Qt 生产者对象、消费者另一线程，数据怎么传？" → Qt 跨线程信号槽。
7. （扩展）"生产者消费者是两个独立 Linux 进程，IPC 怎么选？" → Linux IPC。

**判档**：

| 档位 | 程度 |
|---|---|
| 初级合格 | 数组环形+head/tail 取模+mutex，跑得对、不越界、空/满判定清楚（用 size 或留空槽两种约定能说清） |
| 中级合格 | SPSC 无锁版：`atomic` + `acquire/release` 内存序，且说得出为什么要 acquire/release（可见性+顺序） |
| 中级偏强 | MPMC 说清"不能照搬 SPSC"原因（多写者抢同一 head 要 CAS、ABA）；知道 Michael-Scott 队列、槽级 CAS；主动提 `alignas(64)` 避免伪共享；能讲满/空策略取舍 |

---

## 二、语言基础（初级门槛）

### 1. 指针和引用的区别？什么时候用哪个？

- **考察**：引用语义、生命周期意识
- **初级合格**：引用必须初始化、不能改绑定、无"空引用"概念
- **中级合格**：主动提悬垂引用、生命周期、`const T&` 延长临时对象生命周期

### 2. const 修饰指针/成员函数，有几种？分别什么意思？

- **考察**：`const int*` / `int * const` / `int foo() const`
- **初级合格**：三种能说清

### 3. 类里 static 成员、静态成员函数有什么限制？

- **考察**：无 `this`、不能访问非静态成员
- **初级合格**：能说清

### 4. 写一个 String 类的拷贝构造和赋值

- **考察**：三/五法则、自赋值、`copy-and-swap`
- **初级合格**：写对拷贝构造+赋值、处理自赋值
- **中级合格**：加移动版、`noexcept`、能讲为什么 copy-and-swap 更安全

### 5. new/delete 和 malloc/free 区别？new 失败怎么办？

- **考察**：构造/析构调用、`bad_alloc`、`nothrow`
- **初级合格**：调构造/析构、new 抛 `bad_alloc`
- **中级合格**：`operator new` 重载、placement new、`new[]`/`delete[]` 配对

---

## 三、内存与资源（初级门槛 + 中级分水岭）

### 1. 智能指针有哪些？怎么选？

- **考察**：所有权模型、资源管理
- **初级合格**：知道 `unique_ptr`/`shared_ptr`、能说"防泄漏"
- **中级合格**：unique 默认、shared 仅共享所有权时；主动提**循环引用→`weak_ptr`**、**控制块原子开销**、**线程安全≠对象线程安全**

### 2. 讲讲 RAII，举你代码里的例子

- **考察**：资源管理哲学
- **初级合格**：能背定义"构造获取、析构释放"
- **中级合格**：能举自己代码里"裸指针→unique_ptr"重构的例子、lock_guard 等

### 3. 这个函数返回 std::vector<T> 会不会很低效？为什么？

- **考察**：RVO/NRVO、移动语义
- **初级合格**：能说现代 C++ 不该纠结
- **中级合格**：RVO/NRVO、移动语义、`T&&`，说清"现在不该为返回大对象纠结"

---

## 四、面向对象（初级门槛）

### 1. 虚函数怎么实现的？虚析构为什么必须加？

- **考察**：虚函数表、析构链
- **初级合格**：虚函数表、派生类析构链能讲清
- **中级合格**：多继承下虚表布局、`final`/`override`、纯虚

### 2. 构造函数能是虚的吗？为什么？

- **考察**：经典筛题
- **初级合格**：对象还没建，没虚表指（必答对）

### 3. 多态和函数重载、覆盖、隐藏的区别？

- **考察**：OOP 三态分清
- **初级合格**：三者能分清，分不清的不要

---

## 五、STL（初级）

### 1. vector 扩容机制？push_back 后迭代器什么时候失效？

- **考察**：倍增扩容、失效规则
- **初级合格**：倍增扩容、扩容后所有迭代器失效

### 2. map 和 unordered_map 区别？什么时候用哪个？

- **考察**：红黑树 vs 哈希、有序 vs 无序、最坏复杂度
- **初级合格**：能按场景选对

### 3. 迭代器失效你踩过坑吗？

- **考察**：vector/map 各自失效规则
- **初级合格**：能讲清两类容器失效差异

---

## 六、现代 C++ / 新特性（中级分水岭）

### 1. 移动语义解决什么痛点？移动构造什么时候触发？

- **考察**：右值、`std::move` 本质、引用折叠
- **中级合格**：右值、`std::move` 只是 cast、`T&&` 折叠，讲不清是初级

### 2. lambda 本质是什么？捕获列表 [=]/[&] 的坑？

- **考察**：闭包对象类型、悬垂捕获
- **中级合格**：悬垂捕获、`this` 捕获生命周期坑

### 3. auto、decltype、constexpr、nullptr 你日常怎么用？

- **考察**：是否真在用现代 C++，还是只背名字
- **中级合格**：能举日常用法，不是背概念

### 4. shared_from_this 怎么用？为什么不直接 this？

- **考察**：enable_shared_from_this 经典坑
- **中级合格**：直接 this 会导致独立控制块、双重管理

---

## 七、并发（中级核心，初级能答一题就行）

### 1. 两个线程交替打印 1-100，你怎么写？不用锁能不能？

- **考察**：同步原语熟不熟
- **初级合格**：mutex + condition_variable 干净写出
- **中级合格**：进一步提 `atomic` + `wait/notify_one` 无锁版

### 2. 你遇到过死锁吗？怎么定位、怎么避免？

- **考察**：锁层次、多锁原子获取、调试
- **初级合格**：能背四个必要条件
- **中级合格**：锁层次、`std::lock`/`scoped_lock` 多锁原子获取、超时、日志定位、举真实案例

### 3. atomic 比 mutex 好在哪？什么时候不能用 atomic？

- **考察**：无锁、伪共享、多操作一致性
- **中级合格**：无锁快、缓存行/伪共享；多操作要一致原子性时不能单靠 atomic

---

## 八、Qt（中级核心，4 题）

### 1. 信号槽怎么实现的？MOC 干了什么？

- **初级合格**：知道信号槽、connect
- **中级合格**：MOC 生成元信息、查函数签名、`Q_OBJECT` 宏作用；能区分字符串版（编译期不查错）和**函数指针版**（编译期查错，推荐）

### 2. Qt 对象树，谁负责 delete？new QWidget(parent) 之后能 delete 吗？

- **中级合格**：父对象析构时自动 delete 子对象；**手动 delete 已挂父的对象会 double free**

### 3. 跨线程发信号，连接类型选哪个？忘了选会怎样？

- **中级合格**：`Qt::QueuedConnection`，事件投递到目标线程事件循环；忘选可能直接在调用线程调槽函数踩数据竞争

### 4. QThread 怎么用才算对？继承 QThread 重写 run() vs moveToThread()？

- **中级合格**：业务对象 `moveToThread`、槽函数在该线程跑；继承 QThread 重写 run 是历史误用，答不出别要

---

## 九、Linux（中级核心，4 题）

### 1. epoll vs select/poll？ET 和 LT 怎么选？

- **初级合格**：epoll 不轮询、O(1) 就绪返回
- **中级合格**：ET 边沿触发只通知一次、必须非阻塞读到底；LT 水平触发重复通知更稳；ET 配合 `EPOLLONESHOT`/批量

### 2. fork 之后子进程什么不继承？多线程程序能随便 fork 吗？

- **中级合格**：只继承当前线程、其他线程没跟着 fork；锁状态可能死锁；`pthread_atfork`、或别在多线程里 fork（exec 前做最少事）

### 3. 共享内存 vs 管道 vs 消息队列，你怎么选？

- **中级合格**：大数据高频读写共享内存最快（零拷贝但要自己同步）；管道适合流式小数据父子进程；消息队列有边界自带同步但内核开销

### 4. 程序 coredump 了，你怎么定位？

- **中级合格**：`ulimit -c` 开 core、`gdb ./a.out core`、`bt`、`info threads`、`frame N`；主动提 ASan 更快。光说"看日志"的不行

---

## 十、工程与设计（中级专属，初级答不出属正常）

### 1. 讲一个你重构过的模块，为什么动、怎么动、踩了什么坑？

> 最值钱的一题，看真实经验和权衡能力

### 2. 你什么情况下用继承，什么情况下用组合/模板策略？为什么？

> 考"多用组合少用继承"+ 模板技巧

### 3. 给你一段别人写的屎山，你第一步干什么？

> 好：先读懂不改、加测试兜底、小步重构；差：上来重写

### 4. 你怎么定位一个 C++ 程序变慢了？

> 好：先 profiling（perf/vtune/valgrind），不瞎猜；提缓存命中、内存分配、虚函数调用开销

### 5. 内存泄漏/非法访问你怎么查？

> 好：valgrind/ASan/ASan-in-CI，把工具链写进日常

---

## 十一、高级专项（资深档，5年+）

> 资深档不考语法，考**底层原理、系统设计、复杂权衡、架构决策**。核心判据：能不能在**约束冲突**下做技术选型并讲清代价，有没有**端到端**优化/排查的真实案例。

### A. 语言深度（模板/值类别/内存模型）

#### 1. 讲讲 C++ 内存模型，acquire/release/seq_cst 区别，什么时候用哪个？

- **合格**：能讲 happens-before、release/acquire 配对形成同步、`seq_cst` 全局总序的代价（x86 上贵、ARM 上更贵）；能说"默认 `seq_cst`、性能热点才降序到 `acquire/release`"
- **偏强**：讲清 `seq_cst` 在不同 ISA 上的实现（x86 `mfence` vs ARM `dmb`）、为什么 ARM 上内存序坑多

#### 2. left value / right value / xvalue / prvalue，forwarding reference 和 perfect forward 解决什么？

- **合格**：值类别能分清、`T&&` 在模板推导时折叠成 forwarding reference、`std::forward` 按 `_Tp&` 推导转发左右值
- **偏强**：能讲 `reference collapsing` 四条规则、为什么 `std::move` 和 `std::forward` 实现几乎一样

#### 3. 模板元编程 vs 运行时多态，什么时候用哪个？C++20 concepts 解决了什么？

- **合格**：编译期 vs 运行期、虚函数开销 vs 模板二进制膨胀、concept 替代 SFINAE 让约束可读
- **偏强**：能举自己用模板元编程解决过的真实问题（编译期查表、表达式模板、策略选择），不是只背概念

#### 4. 讲讲链接过程、动态库符号解析、ABI 稳定性你怎么保证？

- **合格**：符号表、动态链接、`dlopen` 延迟绑定、inline 函数跨so的 ODR 问题
- **偏强**：pimpl 惯用法隔离 ABI、版本符号、`-fvisibility`、为什么改成员变量布局就破坏 ABI

### B. 性能与底层（cache/SIMD/分配器/对齐）

#### 1. 你做过最难的性能优化，从 X 到 Y，怎么做到的？

> 最值钱的高级题——看真实优化经验和性能直觉

- **合格**：有从 profiling 到瓶颈定位到优化的完整链路，能讲 cache miss / 分配 / 分支预测等具体原因
- **偏强**：能讲优化前后的取舍（用空间换时间、可读性换性能）、回归测试、基线监控

#### 2. cache 友好的数据结构怎么设计？举个你重构过的例子

- **合格**：AoS vs SoA、`alignas(64)` 避免伪共享、按访问局部性排布、缓存行预取
- **偏强**：能讲 TLB、大页、NUMA 亲和性、预取指令、`__builtin_prefetch`

#### 3. 什么时候用自定义分配器？jemalloc/tcmalloc 比 malloc 好在哪？

- **合格**：固定大小对象池、arena、对象生命周期已知、减少碎片和锁竞争
- **偏强**：讲 tcache、per-thread arena、多线程下 glibc malloc 的锁瓶颈

#### 4. SIMD 你用过吗？什么时候手写、什么时候靠编译器自动向量化？

- **合格**：能说 `__m256i`、`#pragma omp simd`、`-march=native`，知道什么时候自动向量化有效（连续内存、无依赖）
- **偏强**：讲过具体优化案例、限制（分支、gather 慢）

### C. 并发深度（无锁/内存序/同步原语）

#### 1. 实现一个无锁 MPMC 队列，ABA 怎么处理？

- **合格**：数组槽 CAS、或 Michael-Scott 链表、ABA 用 tag/version 或 hazard pointer/epoch GC
- **偏强**：讲清无锁(lock-free) vs 无等待(wait-free)的区别和代价、什么时候不该上无锁（复杂度/可维护性 > 收益）

#### 2. futex 是什么？mutex 在用户态和内核态怎么切换？

- **合格**：fast path 用户态 CAS、slow path 陷入内核 `futex` 系统调用、为什么多数情况不进内核
- **偏强**：能讲优先级继承、`robust mutex`、自旋+休眠混合策略

#### 3. 一个高并发计数器你怎么实现？为什么不能简单 atomic++？

- **合格**：`atomic` 上的竞争导致 cache line 弹来弹去；用 per-thread 计数 + 合计读
- **偏强**：讲 `relaxed` 序就够、读时聚合、NUMA 下按节点分片

### D. 系统与运行时

#### 1. 虚拟内存、页表、TLB、大页，对你的程序有什么实际影响？

- **合格**：缺页、TLB miss 代价、大页减少 TLB 项、`mmap` 做文件映射/匿名映射
- **偏强**：透明大页 THP 的坑、`madvise`/`MAP_HUGETLB`、脏页回写与延迟

#### 2. 一个服务 QPS 从 1k 涨到 100k，你怎么设计？瓶颈在哪？

> 系统设计题——看架构决策能力

- **合格**：单机瓶颈（CPU/IO/锁/连接数）、水平扩展、无状态化、缓存、异步化、连接复用
- **偏强**：能讲全链路——网关层、应用层、存储层、缓存层各瓶颈和对策、降级/熔断/限流、容量规划

#### 3. io_uring / epoll / 阻塞 IO，你怎么选？零拷贝你用过哪些？

- **合格**：epoll 适合连接多、io_uring 适合高 IOPS、`sendfile`/`splice` 零拷贝
- **偏强**：能讲 io_uring 的 submission/completion queue、批量化、和 Reactor/Proactor 模式的关系

#### 4. NUMA 对你的程序有影响吗？你怎么处理？

- **合格**：跨 node 访问内存慢、`numactl`/CPU 亲和、内存本地分配
- **偏强**：per-node 分片、避免跨 node 共享热点

### E. 架构与设计决策

#### 1. 讲一个你主导设计的系统，关键决策是什么？为什么那么选？代价是什么？

> 资深核心题——看架构权衡，对应软考案例的"架构决策"题

- **合格**：有真实主导案例、能讲 2-3 个关键决策的备选方案和取舍
- **偏强**：能讲决策的长期后果（可扩展性/可维护性/技术债）、在约束（人/时间/存量）下的妥协

#### 2. 你怎么管理技术债？什么时候还、什么时候欠？

- **合格**：有意识区分"短期债/长期债"、有量化（复杂度/改动成本）、能定还债优先级
- **偏强**：能把技术债和业务价值挂钩、说服非技术方投入还债、建立守护机制

#### 3. 一个模块要重写还是演进，你怎么决策？

- **合格**：先评估"屎山是否还有价值、重写的真实成本（含隐式需求/测试覆盖）"、倾向渐进重构
- **偏强**：能讲"重写二必败"的反模式、strangler fig 模式、灰度迁移

### F. 调优与排查（现场题）

#### 1. 线上服务偶发毛刺（P99 抖动），你怎么排查？

- **合格**：火焰图/perf 找热点、查 GC/分配/锁竞争/IO、关联部署变更
- **偏强**：能用 eBPF/perf events 抓运行时、区分应用层/内核层/调度层原因

#### 2. 程序 OOM 了，但没明显泄漏，你怎么定位？

- **合格**：`/proc` 看内存去向、`smem`/`pmap`、RSS vs PSS、临时内存峰值、碎片
- **偏强**：讲内存碎片导致的虚高、`malloc_trim`、`MALLOC_ARENA_MAX` 调优、cgroup 限制

#### 3. 一个多线程程序偶发 crash，复现概率低，你怎么搞？

- **合格**：ASan/TSan、core dump 反复 `bt`、加日志二分、压测复现
- **偏强**：用 TSan 抓数据竞争、model checker、压力测试边界条件（CPU 数量、调度顺序）

---

## 附录：面试官判档速查

| 信号 | 档位推断 |
|---|---|
| 能写对拷贝构造+智能指针但不深讲、vtable 说得清 | 初级合格 |
| 能讲移动语义/lambda/并发 + **能讲代价与权衡** | 中级合格 |
| 有真实重构/调优案例 + 能结合项目讲取舍 + 工具链熟 | 中级偏强 |
| 只背答案、不会讲代价、不会讲"什么时候不适用" | 偏初级 |
| 能讲内存模型/无锁/ABI 底层 + 有端到端性能优化或排查案例 | 资深合格 |
| 能讲主导系统设计的关键决策和长期代价 + 能在约束冲突下做技术选型 | 资深偏强 |

**一句话标准**：能讲清"为什么用 A 不用 B、A 的代价是什么"的是中级；只会背答案的是初级。
